server:
  port: 8086

spring:
  application:
    name: media-service

  datasource:
    url: ${MEDIA_DB_URL:jdbc:postgresql://localhost:5432/nexa_media_db}
    username: ${MEDIA_DB_USERNAME:postgres}
    password: ${MEDIA_DB_PASSWORD:admin}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  config:
    import: "optional:configserver:http://localhost:8888/"

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_SERVER:http://localhost:8761/eureka/}